Installation
############

The package is published on `PyPI <https://pypi.org/project/{{ project_slug }}/>`_ and can be installed with `pip` (or any equivalent):

.. code-block:: bash

   pip install {{ project_slug }}
